apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-ingress
  annotations:
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  rules:
    - host: flower_worker
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flower_worker-service
                port:
                  number: 5555
