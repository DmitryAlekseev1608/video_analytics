apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery_worker-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: celery_worker-app
    spec:
      containers:
        - name: celery_worker-videoanalytics
          image: checkpoint6/django:latest
          command: ['celery', '-A', 'backend.celery_app', 'worker']
          volumeMounts:
            - mountPath: "/var/lib/postgresql/data"
              name: postgres-storage
            - mountPath: "/data"
              name: django-storage
          imagePullPolicy: Never
          envFrom:
            - secretRef:
                name: django-secret
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc-postgres-statefulset-0
        - name: django-storage
          emptyDir:
            sizeLimit: 1000Mi
  selector:
    matchLabels:
      app: celery_worker-app
